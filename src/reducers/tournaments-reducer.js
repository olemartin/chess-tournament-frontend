const initialState = {
    tournaments: [],
};

export default function tournamentsReducer(state = initialState, action) {
    switch (action.type) {
        default: {
            return state;
        }
    }
}
